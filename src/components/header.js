// Import libraries fore making a component
import React from "react";
import { Text } from "react-native";

const Header = () => {
    return (
        <Text>Albums!</Text>
    );
};

export default Header;
